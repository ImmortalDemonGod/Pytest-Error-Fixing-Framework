# Bug catalog — pef-p09-resolve-async-sync-mismatch

| # | Location | Defect | Evidence (harness-executed) | Caught by |
|---|----------|--------|------------------------------|-----------|
| 1 | src/branch_fixer/storage/recovery.py:135,174,225 | RecoveryManager.create_checkpoint (line 135), restore_checkpoint (line 174), and handle_failure (line 225) are all declared as `async def`. In orchestrator.py, both callers are synchronous: (1) _create_checkpoint_if_needed at line 516 calls `checkpoint = self.recovery_manager.create_checkpoint(session, metadata)` without await — receives a coroutine object, not a RecoveryPoint; the subsequent `checkpoint.id` at line 518 raises AttributeError which is NOT caught by the `except CheckpointError` handler, so the exception propagates unhandled. (2) handle_error at line 393 calls `recovered = self.recovery_manager.handle_failure(error, self._session, context)` without await — receives a truthy coroutine object; `if recovered:` at line 396 always evaluates True, so handle_error always returns True falsely reporting recovery success without any recovery work being done. Tests in test_recovery.py lines 134-198 confirm the methods are async (all use @pytest.mark.asyncio and await), while test_orchestrator.py lines 565-598 mock the recovery manager with synchronous stubs — masking the mismatch. | orchestrator._create_checkpoint_if_needed receives a RecoveryPoint so `checkpoint.id` works (no AttributeError); handle_error returns True only when real recovery ran, not because a coroutine object is truthy; recovery tests pass without asyncio markers. | `tests/test_pef_p09_resolve_async_sync_mismatch.py` |

- **Expected (per the finding goal):** orchestrator._create_checkpoint_if_needed receives a RecoveryPoint so `checkpoint.id` works (no AttributeError); handle_error returns True only when real recovery ran, not because a coroutine object is truthy; recovery tests pass without asyncio markers.
- Every row above is recorded ground truth (finding fields + harness-executed command outputs); no value is estimated.
